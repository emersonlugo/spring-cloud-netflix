server:
  port: ${PORT:8081}

security:
  basic:
    enabled: false

zuul:
  routes:
    client:
      path: /api-client/**
      serviceId: CLIENT-SERVICE
      
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000